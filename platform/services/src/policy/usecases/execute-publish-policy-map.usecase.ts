/**
 * ExecutePublishPolicyMap - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: policy
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecutePublishPolicyMap Use Case
 *
 * DDD: Application use case for get Publish.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { PublishPolicyMapInput, PublishPolicyMapOutput } from "../dto/publish.dto";
import type { ExecutionContextService, IdGeneratorService } from "@scorvia/services/_shared/index.js";
import type { PolicyMapRepository, PublishPublisher } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecutePublishPolicyMap {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly publish: PublishPublisher,
    private readonly policyMap: PolicyMapRepository
  ) {}

  async execute(input: PublishPolicyMapInput): Promise<PublishPolicyMapOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.polId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).policyMapId) {
      throw new ValidationError("policyMapId is required");
    }
    const policymapId = (input as any).policyMapId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.publish.publishPolicyMap({ policyMapId: policymapId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${policymapId}`);
    }

    return entity as PublishPolicyMapOutput;
  }
}
