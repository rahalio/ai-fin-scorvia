/**
 * ExecuteRunEvaluation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evaluations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRunEvaluation Use Case
 *
 * DDD: Application use case for create Evaluation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RunEvaluationInput, RunEvaluationOutput } from "../dto/evaluation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@scorvia/services/_shared/index.js";
import type { EvaluationRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteRunEvaluation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly evaluation: EvaluationRepository
  ) {}

  async execute(input: RunEvaluationInput): Promise<RunEvaluationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.evlId();

    // All field names extracted from OpenAPI spec: includeBiasChecks, includeRegimeTests, requireImbalanceMetrics
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    // No required field validations    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (evaluations domain)
    const entityId = this.idGenerator.evlId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.evaluation.runEvaluation({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as RunEvaluationOutput;
  }
}
