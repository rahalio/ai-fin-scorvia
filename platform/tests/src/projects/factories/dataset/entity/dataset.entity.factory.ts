/**
 * Dataset Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { DatasetEntity } from "@scorvia/core/projects/models/index.js";
import { projectsSchemas } from "@scorvia/core/projects";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "prj";

export function createDataset(overrides: Partial<DatasetEntity> = {}) {
  const raw = {
    id: overrides.id ?? `dst_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    name: overrides.name ?? faker.person.fullName(),
    projectId: overrides.projectId ?? `prj_${ulid().toLowerCase()}`,
    tenantId: overrides.tenantId ?? `tnt_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return projectsSchemas.Dataset.parse(raw);
}

export function datasetBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createDataset(overrides));
}
