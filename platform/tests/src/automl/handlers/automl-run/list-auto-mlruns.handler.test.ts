/**
 * ListAutoMLRuns handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: automl
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listAutoMLRuns } from "@scorvia/api-server/domains/automl/handlers";
import type { AutomlDDDDependencies } from "@scorvia/api-server/domains/automl";
import { createAutomlRun as createAutomlRunEntity } from "@scorvia/factories/automl";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@scorvia/tests/shared";
import { AUTOML_USE_CASES } from "../__factories__/use-cases.js";

describe("ListAutoMLRuns", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AutomlDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AutomlDDDDependencies>(
      AUTOML_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createAutomlRunEntity({ orgId }),
      createAutomlRunEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListAutoMLRuns, "execute").mockResolvedValue(useCaseResult);

    await listAutoMLRuns(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListAutoMLRuns.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
