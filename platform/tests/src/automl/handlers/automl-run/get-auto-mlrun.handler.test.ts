/**
 * GetAutoMLRun handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: automl
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAutoMLRun } from "@scorvia/api-server/domains/automl/handlers";
import type { AutomlDDDDependencies } from "@scorvia/api-server/domains/automl";
import { createAutomlRun as createAutomlRunEntity } from "@scorvia/factories/automl";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@scorvia/tests/shared";
import { AUTOML_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAutoMLRun", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AutomlDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AutomlDDDDependencies>(
      AUTOML_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createAutomlRunEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAutoMLRun, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { runId: entity.id };

    await getAutoMLRun(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAutoMLRun.execute).toHaveBeenCalledWith(expect.objectContaining({ runId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { runId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAutoMLRun, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAutoMLRun(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
