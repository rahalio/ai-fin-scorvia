/**
 * ListPolicyMaps handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: policy
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listPolicyMaps } from "@scorvia/api-server/domains/policy/handlers";
import type { PolicyDDDDependencies } from "@scorvia/api-server/domains/policy";
import { createPolicyMap as createPolicyMapEntity } from "@scorvia/factories/policy";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@scorvia/tests/shared";
import { POLICY_USE_CASES } from "../__factories__/use-cases.js";

describe("ListPolicyMaps", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PolicyDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PolicyDDDDependencies>(
      POLICY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createPolicyMapEntity({ orgId }),
      createPolicyMapEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListPolicyMaps, "execute").mockResolvedValue(useCaseResult);

    await listPolicyMaps(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListPolicyMaps.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
