/**
 * GetEvaluation handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evaluations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getEvaluation } from "@scorvia/api-server/domains/evaluations/handlers";
import type { EvaluationsDDDDependencies } from "@scorvia/api-server/domains/evaluations";
import { createEvaluation as createEvaluationEntity } from "@scorvia/factories/evaluations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@scorvia/tests/shared";
import { EVALUATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetEvaluation", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: EvaluationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<EvaluationsDDDDependencies>(
      EVALUATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createEvaluationEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetEvaluation, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { evaluationId: entity.id };

    await getEvaluation(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetEvaluation.execute).toHaveBeenCalledWith(expect.objectContaining({ evaluationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { evaluationId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetEvaluation, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getEvaluation(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
