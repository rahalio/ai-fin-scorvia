{
  "name": "scorvia",
  "version": "0.1.0",
  "description": "Scorvia — governed AutoML credit-decision factory",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "lint": "eslint .",
    "lint:openapi": "pnpm --filter @scorvia/openapi-core lint:domains",
    "bundle:openapi": "pnpm --filter @scorvia/openapi-core bundle:domains",
    "build": "pnpm --filter @scorvia/core build && pnpm --filter @scorvia/services build && pnpm --filter @scorvia/adapters build && pnpm --filter @scorvia/api-server build",
    "dev:api": "pnpm --filter @scorvia/api-server dev",
    "codegen:core": "node scripts/sync-codegen-paths.mjs && PYTHONPATH=.codegen/codegen/src python3 -m zero_codegen.cli.main generate --all --layers core --config .codegen/.zero-codegen-merged.json --skip-build",
    "codegen:identity": "node scripts/sync-codegen-paths.mjs && PYTHONPATH=.codegen/codegen/src python3 -m zero_codegen.cli.main generate --domain identity --config .codegen/.zero-codegen-merged.json --skip-build",
    "codegen:paths": "node scripts/sync-codegen-paths.mjs",
    "dev": "pnpm --filter @scorvia/api-server --filter @scorvia/webapp --parallel dev",
    "dev:web": "pnpm --filter @scorvia/webapp dev",
    "build:web": "pnpm --filter @scorvia/webapp build"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.55.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9.0.0"
  }
}
